# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: hexin_ai_ui
description: Reusable AI card components for Hexin applications.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  genui:
    path: ../genui
  hexin_dsl:
    path: ../hexin_dsl
  json_schema_builder:
    path: ../json_schema_builder
  intl: ^0.19.0
  flutter_markdown_plus: ^1.0.2
  webview_flutter: ^4.10.0

dependency_overrides:
  flutter_markdown_plus: 1.0.0+1
  webview_flutter: 4.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter


