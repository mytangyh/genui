name: hexin_speech
description: Speech recognition package for Hexin AI using sherpa-onnx for offline ASR
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  logging: ^1.2.0
  path: ^1.8.0
  path_provider: ^2.1.0
  record: ^5.1.0
  sherpa_onnx: ^1.12.23
  uuid: ^4.3.3

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
